// Common global constants and operations.
#ifndef GLOBALS_H
#define GLOBALS_H

#define MAX_DEPTH   6			//Maximum recursion depth
#define EPSILON     0.001f    //Very small value to avoid artifacts
#define INFINITY    10000000.0f  //Very large value used instead of infinity

#define Max(A,B)    (A>B ? A : B)	//Returns the larger value
#define Min(A,B)    (A<B ? A : B)	//Returns the smaller value



#endif